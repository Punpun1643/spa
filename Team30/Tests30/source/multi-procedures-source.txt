procedure Bumblebee {
  read x;
  y = 2 * z;
  z = p - y;

  if (q != 1) then {
    z = x + 24;
  } else {
    while (z > 0) {
        y = x * q - 5;
        z = z - 1;
    }
  y = x + y * z + p * q;
  }

  i = x + j + z;
}


procedure Megatron {
  p = x * 10;
  q = y + y * 10;
  
  while (t == 0) {
    a = 5 * p - 5;
    print y;
  }

  q = p * 5;
}

procedure IronHide {
  i = x * 10;
  j = x + y * 10;
  if (i > j) then {
    read x;
  } else {
    y = i + x + y * z;
  }
}

procedure Barricade {
    q = i * x * j + y + p * q;
    print q;
}